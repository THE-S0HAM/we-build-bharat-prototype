"""Registration lookup tools for agents.

These are the typed tool interfaces that agents invoke to
look up transactional facts. Agents must use these tools —
they cannot access DynamoDB directly.
"""
